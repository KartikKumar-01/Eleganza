<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Eleganza</title>
    <link rel="stylesheet" href="/styles/output.css" />
    <link rel="stylesheet" href="/styles/styles.css">
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <div
      class="text-accent p-10 bg-primary min-w-full min-h-screen flex flex-col font-sans justify-start items-center"
    >
    <% if (success && success.length > 0) { %>
  <div class="flash flash-success"><%= success %></div>
<% } %>

<% if (error && error.length > 0) { %>
  <div class="flash flash-error"><%= error %></div>
<% } %>

      <div class="heading font-semibold text-[48px]">
        <p>
          Welcome to
          <span
            class="bg-radial-blue drop-shadow-custom-25 bg-clip-text text-transparent text-[70px]"
            >Eleganza</span
          >
        </p>
      </div>
      <div class="cards mt-5 w-[60%] flex justify-between">
        <div class="card" id="sign-up-card">
          <h1 class="text-[25px] font-thin">Create Your Account</h1>
          <form
            class="space-y-4 mt-3"
            id="sing-up-form"
            action="/user/register"
            method="post"
            autocomplete="off"
          >
            <input
              type="text"
              class="form-input"
              placeholder="Full Name"
              name="fullname"
            />
            <input
              type="email"
              class="form-input"
              placeholder="Email"
              name="email"
            />
            <input
              type="password"
              class="form-input"
              placeholder="Password"
              name="password"
            />
            <input
              type="submit"
              class="form-button"
              value="Create my account"
            />
          </form>
        </div>
        <div
          class="circle flex items-center justify-center rounded-[100%] self-center w-[70px] h-[70px]"
        >
          <p class="font-semibold text-[25px]">OR</p>
        </div>
        <div class="card" id="sing-up-card">
          <h1 class="text-[25px] font-thin">Login Your Account</h1>
          <form
            id="sing-up-form"
            class="space-y-4 mt-3"
            action="/user/login"
            method="post"
            autocomplete="off"
          >
            <input
              type="email"
              class="form-input"
              placeholder="Email"
              name="email"
            />
            <input
              type="password"
              class="form-input"
              placeholder="Password"
              name="password"
            />
            <input type="submit" class="form-button w-[100px]" value="Log in" />
          </form>
        </div>
      </div>
    </div>
    <script src="/javascript/flash.js"></script>
  </body>
</html>
